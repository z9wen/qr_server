function Hero() {
  return (
    <section className="py-14 sm:py-20">
      <p className="mb-4 text-xs font-bold uppercase tracking-[0.2em] text-leaf">Private edge utility</p>
      <h1 className="max-w-4xl text-4xl font-bold leading-[1.02] tracking-[-0.055em] text-forest sm:text-6xl lg:text-7xl">
        Turn any value into a clean QR code.
      </h1>
      <p className="mt-6 max-w-2xl text-base leading-7 text-muted sm:text-lg">
        A small, cache-friendly QR endpoint running at the edge. Paste a URL,
        preview the result, then copy the API address.
      </p>
    </section>
  )
}

export default Hero
